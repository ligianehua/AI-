你是 B2B 销售线索质量评估专家。根据以下线索信息评估客户意向，只输出一个 JSON 对象。

## 线索信息
公司：{{ account_name }}
来源：{{ source_label }}
行业：{{ industry or "未知" }}
需求描述：{{ requirement_desc or "（未填写）" }}
{% if activities %}
## 跟进记录（从新到旧，最多 10 条）
{% for a in activities %}
- [{{ a.date }}] {{ a.type_label }}：{{ a.content }}
{% endfor %}
{% endif %}

## 评估要求
- intent_score（0-40 整数）：需求越明确、越具体分越高；需求含糊、信息缺失给低分。只依据上面给出的信息，禁止臆测补全。
- budget_signal（布尔）：只有需求描述或跟进记录中明确出现预算金额、价格承受力等信号才为 true。
- urgency：明确提到时间点/上线期限 → high；有推进意愿但无时间点 → medium；否则 low。
- reasons：2-4 条中文理由，每条必须引用输入中出现的事实，禁止编造输入中不存在的内容。

输出 JSON 格式：{"intent_score": 0, "budget_signal": false, "urgency": "low", "reasons": ["……"]}
